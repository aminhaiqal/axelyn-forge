{
  "name": "axelyn-forge-workspace",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @axelyn/web",
    "check:web": "npm run check --workspace @axelyn/web",
    "build:web": "npm run build --workspace @axelyn/web"
  }
}
